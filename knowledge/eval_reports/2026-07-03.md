# Weekly evaluation — 2026-07-03

**Snapshot:** `eval-inbox/tp_snap_2026-07-01.db` (captured 2026-07-01; 2 days old — freshness gate PASS, < 8d)
**Hermes input:** `eval-inbox/hermes-issues.json` (2 issues, #63 + #67, both `do-not-auto-implement`)
**Prior report:** `knowledge/eval_reports/2026-07-01.md`

## TL;DR

**Tickets written this week: 0.** `.ticket-counter` unchanged (still 1009).

**⚠️ Inputs are byte-for-byte identical to last week's eval.** The newest snapshot is the *same* `tp_snap_2026-07-01.db` the 2026-07-01 report already processed (same 864 decisions, same 5 trades, newest decision still id 864 @ 2026-07-01 14:04), and the Hermes file is the same #63/#67 pair. No new bot activity or issue has landed since the last run. It still passes the freshness gate, so this is a legitimate — not aborted — "nothing new" week. **Process note for David:** if next week's run sees this same 2026-07-01 snapshot again, the Fri 21:00 `pull-eval-inputs.ps1` pull is stalled and should be checked — a frozen input would silently make every eval a no-op.

**Good news — both of last week's open repo items are now fixed in current code:**
1. **`claude-sonnet-5` pricing is now in `CLAUDE_PRICING`** (config.py:985–993, real rates: input 2.00 / output 10.00 / cache_read 0.20 / cache_write 4.00, with an intro-pricing note and a 2026-09-01 update reminder). Last week this was an open `TODO(david)` and I flagged it report-only pending your verified rates. **Resolved — no ticket needed.**
2. **`ADVISOR_MAX_TOKENS` default is 4096** (config.py:331) and the code now carries an explicit comment (config.py:328–330) documenting the sonnet-5 truncation rationale. The code side was already correct last week; the truncation was a stale Render env override, which I can't see from a frozen snapshot (see operator action below).

---

## Step 1 — DB analysis (unchanged from 2026-07-01)

### Funnel (decisions)

| Window | Total | Actions |
|---|---|---|
| All-time (since 2026-04-13) | 864 | SKIP 857, SELL_PUT 7 |
| Trailing 7d | 146 | SKIP 146 (0 trade actions) |

**Skip reasons, trailing 7d:** structurally_untradeable 102 (70%), claude_skip 39 (27%), no_candidates_found 5 (3%).
**By strategy (7d):** wheel 71, turnover_wheel 70, iron_condor 3, bull_put_spread 2 — all SKIP. `structurally_untradeable` (pre-check gate) remains the dominant funnel loss — the known watchlist/structural baseline, not a defect.

### Fills

**Zero fills, still.** `trades` = 5 rows total (4 canceled, 1 pending), submitted 2026-05-19 → 2026-06-12; none filled. Zero in the trailing 7d and zero across the bot's life. Baseline **unchanged**.

### Cost (token_usage, trailing 7d)

- Total **$1.28** (all-time $13.47).
- `claude-sonnet-4-6` $1.04 (35 calls); `claude-sonnet-5` $0.24 (4 calls). Identical to last week.

### Health flags

- **`max_tokens` truncation: 4 decisions** (ids 853/854/863/864), all sonnet-5, all from 2026-07-01 — the same four as last week. No *new* truncations (there's no new data). Code default is 4096; the live truncation, if still occurring, is a Render `ADVISOR_MAX_TOKENS=2048` env override — unverifiable from this frozen snapshot. Operator action stands (below).
- **Orphan trade-action decisions: 2** (id 183 F, id 209 VZ — both SELL_PUT, 2026-05-13/14, no `trades` row). Same stale pair; pre-dates the 2026-05-19 context-builder isolation fix. Execution/recording territory (red scope), n=2, stale → report-only.
- **decisions.estimated_cost_usd NULL for all rows** — already handled (`CLAUDE_COSTS_SOURCE=token_usage`, config.py:335). No action.
- **Circuit breaker:** no anomalies; skips are pre_check/claude, not CB.

---

## Step 2 — Hermes issues

#63 (2026-06-30) and #67 (2026-07-01), both "[hermes] macro: propose missing FOMC/CPI dates", both `do-not-auto-implement`, near-duplicates → **deduped to one candidate → REPORT ONLY.** Same pair the 2026-07-01 report already verified. No new Hermes issues this week. Verified-date guidance from last week stands and is not repeated here (see `2026-07-01.md`, Step 2): near-term coverage is intact; 2027 CPI and post-Jan-2028 FOMC dates remain unverifiable extrapolations until BLS/Fed publish official schedules. **Hold both.**

---

## Step 3–4 — Triage outcome

| Candidate | Scope | Grounded in current code | Outcome |
|---|---|---|---|
| sonnet-5 pricing missing in repo | cost/observability | **Now fixed** (config.py:985) | Closed — no ticket |
| sonnet-5 max_tokens truncation | config/env | Default already 4096 (config.py:331) | Operator env action (Render) — report-only |
| decisions cost columns NULL | observability | Already handled (config.py:335) | Drop |
| Hermes macro dates #63/#67 | data | `do-not-auto-implement` | Report-only (held) |
| Orphan SELL_PUT decisions (2) | execution/recording | Stale, red scope, n=2 | Report-only |
| structurally_untradeable dominance | funnel baseline | Known structural constraint | Context only |

Every green-eligible candidate is either already fixed in code or has no new evidence. **No green ticket surfaced. 0 tickets — the correct outcome.**

---

## Items awaiting David's decision (red / report-only)

1. **[Operator/env — verify]** Confirm Render `ADVISOR_MAX_TOKENS` is ≥ 4096 (code default is 4096). I cannot see live env from the frozen 2026-07-01 snapshot; if the live advisor is still force-SKIPping sonnet-5 calls on `max_tokens`, this env override is the cause. Check once a fresh snapshot lands.
2. **[Process — watch]** Confirm the Fri 21:00 `pull-eval-inputs.ps1` pull is running. This eval saw the identical 2026-07-01 snapshot as last week; a repeat next week means the pull is stalled.
3. **[Report-only]** Hermes #63/#67 — hold; forward FOMC-2028 / CPI-2027 dates unverifiable until officially published (details in `2026-07-01.md`).

## Notes

- Notification written to `eval-inbox/eval-notify.json` per the methodology (the sandbox cannot reach ntfy/SMTP; `notify.py` is intentionally NOT called from here). The local `trade-pilot eval-notify` poller sends the push + email.

# Weekly eval — 2026-09-04 — ABORTED (stale inputs, 65 days; 7th consecutive abort on the same snapshot)

Freshness gate failed. No evaluation performed, no tickets written, `.ticket-counter` untouched.

## Input state

| Item | Value |
|---|---|
| Newest snapshot | `eval-inbox/tp_snap_2026-07-01.db` |
| Snapshot age | **65 days** (limit: 8) |
| `hermes-issues.json` | dated 2026-07-01, md5 `355455911bad72cf01de206d2dd9ca7c` — byte-identical to every prior week |
| Other snapshots anywhere in repo | none (`find . -name "tp_snap_*.db"` → 1 result) |
| Tickets written | 0 |
| `tickets/.ticket-counter` | `1009` (untouched) |
| `tickets/ready/` | empty |

Per `scripts/eval/weekly-eval.md` Step 0, stale data is not evaluated. Nothing in this report
makes any claim about the bot's funnel, fills, cost, token truncation, or circuit-breaker state.

## The notification channel is confirmed dead for a third week

`eval-inbox/eval-notify.json` written by the **2026-08-28** run is still on disk, unconsumed —
mtime `2026-08-28 19:07`, **6 days old** at the time of this run.

`scripts/eval/send-eval-notification.ps1:25` deletes that file after a successful send
(`Remove-Item $File -Force  # fire once`). The only path that leaves it in place is the `catch`
block. A third consecutive generation surviving a full week means the poller is either not
firing at all or failing on every attempt.

**David has now almost certainly missed the 08-14, 08-21, and 08-28 pushes.** That is the direct
cause of the recommended operator actions never being carried out three weeks running — the
reports were written, the alarm never rang. This remains the single highest-priority failure in
the loop: an eval nobody reads is an eval that does not exist.

This run overwrites the same file. If the poller is still dead, this push will not be delivered
either, and **this report file is the primary artifact.**

Sandbox network confirmed unreachable again, independently: DNS resolution for `ntfy.sh`,
`smtp.gmail.com`, and `api.github.com` all returned `Temporary failure in name resolution`.
`notify.py` was **not** invoked (per `weekly-eval.md`, which documents this sandbox cannot send)
— no spurious notification escaped from here.

## Failure inventory — three jobs down, unchanged from last week

| Job | Where | Last known good | Status |
|---|---|---|---|
| `pull-eval-inputs.ps1` (Fri 21:00) | Task Scheduler | 2026-07-01 | **Broken 9 weeks** |
| `trade-pilot-weekly-eval` (Fri 22:00) | Cowork scheduled task | 2026-09-04 (this run) | **Running** — fired 08-14, 08-21, 08-28, 09-04; gaps at 07-31, 08-07 |
| `send-eval-notification.ps1` (polling) | Task Scheduler | unknown, ≥ 2026-08-14 | **Broken ≥ 3 weeks** (three stale `eval-notify.json` generations) |

Aborted evals on the identical July 1 snapshot: 07-10, 07-17, 07-24, 08-14, 08-21, 08-28,
**09-04**.

The only component reliably working is the Cowork eval task itself — the one component that can
do nothing useful without the other two.

## Ambiguous signal, offered without a conclusion

The local working copy `data/trade_pilot.db` has mtime `2026-07-03`, and `data/snapshots/*.json`
range from April to July 3. This is **expected** if the bot runs on Render against its own
persistent disk and the local repo is only a dev checkout — it is **not** evidence the bot is
down, and should not be read as such. It is noted only because it is the same date the pull job
stopped, which is worth one look while diagnosing: if `pull-eval-inputs.ps1` copies from a local
path rather than pulling from Render, the two facts have a single cause.

## Recommended operator actions (in order — unchanged from 08-21 and 08-28, still not done)

1. **Fix the notification first.** Run `scripts\eval\send-eval-notification.cmd` by hand and read
   the output. If it throws, `notify.py` is at fault (check the ntfy topic and SMTP credentials
   in `.env`); if it exits silently with `eval-notify.json` still present, the Task Scheduler
   entry is not firing. Until this works, every future eval is invisible.
2. Run `scripts\eval\pull-eval-inputs.ps1` by hand and capture the error. It stopped cleanly on
   July 1 — most likely the Render/production DB path or credential, an expired GitHub token for
   the Hermes pull, or a moved destination path.
3. Check Task Scheduler history for both the pull job and the notify poller.
4. Once fresh inputs land, re-run this eval **manually** — do not wait for Friday. Nine weeks of
   decisions are unexamined.

## Health / risk note

**Any regression since 2026-07-01 is currently undetected.** A change in fill behaviour, a cost
blowout, a circuit-breaker trip, or a silent scheduler crash would all look identical from here,
which is to say invisible. The bot should be treated as **unmonitored for nine weeks**. Nothing
in this report argues it is healthy; it argues we still cannot tell.

## Red-scope / do-not-auto-implement items

None newly triaged. Hermes issues were not re-evaluated — the input file is byte-identical to
content already reviewed in prior weeks. The July 1 macro-date issue carrying
`do-not-auto-implement` (FOMC/CPI schedule extrapolation, unverified) remains report-only and
unresolved, awaiting David's decision.

## Carried forward (unchanged, still open)

- `tickets/attended/1007-investigate-vz-position-no-trade-record.md`
- `tickets/attended/1008-decide-monthly-eval-retirement.md`

Both are human-gated and were not touched by this run.

# Weekly evaluation — 2026-07-10

**ABORTED — Step 0 freshness gate FAIL. No DB analysis, no Hermes triage, no tickets.**

## Why aborted

- Newest snapshot in `eval-inbox/` is `tp_snap_2026-07-01.db`, captured **2026-07-01 = 9 days old** as of today (2026-07-10). The gate stops any eval when the newest snapshot is > 8 days old. **9 > 8 → STOP.** Evaluating a 9-day-stale snapshot would produce misleading funnel/cost/fill numbers, so per methodology Step 0 nothing downstream ran.
- No newer `tp_snap_*.db` exists; `hermes-issues.json` is also unchanged (same #63/#67 pair). `.ticket-counter` untouched at **1009**.

## Root cause — the stalled pull, now confirmed

The **2026-07-03 report explicitly predicted this**: it flagged that the eval had seen the *identical* `tp_snap_2026-07-01.db` two weeks running and warned "if next week's run sees this same 2026-07-01 snapshot again, the Fri 21:00 `pull-eval-inputs.ps1` pull is stalled and should be checked." That is exactly what happened — a third consecutive week on the same frozen input, and it has now aged past the freshness gate. **The `pull-eval-inputs.ps1` Task Scheduler job (Fri 21:00) is not producing fresh snapshots and needs operator attention.**

## Operator actions (David)

1. **[Blocking — fixes the loop]** Investigate the Fri 21:00 `scripts/eval/pull-eval-inputs.ps1` Windows Task Scheduler job. It has not written a new `tp_snap_*.db` since 2026-07-01 (three weeks). Until it produces a fresh snapshot, every weekly eval will keep aborting on the freshness gate. Check: Task Scheduler last-run result/history, the script's DB export path/permissions, and whether the source SQLite is reachable from the scheduled context.
2. **[Carried over, still open]** Confirm Render `ADVISOR_MAX_TOKENS` ≥ 4096 (code default is 4096 at config.py:331). The four sonnet-5 `max_tokens` truncations in the 2026-07-01 snapshot may be a live Render env override; unverifiable until a fresh snapshot lands.
3. **[Report-only, held]** Hermes #63/#67 macro-date issues remain `do-not-auto-implement`; forward FOMC-2028 / CPI-2027 dates unverifiable until BLS/Fed publish official schedules (details in `2026-07-01.md`).

## Notes

- Notification written to `eval-inbox/eval-notify.json` per methodology (sandbox cannot reach ntfy/SMTP; `notify.py` intentionally NOT called from here — the local `trade-pilot eval-notify` poller sends the push + email).
- No files written except this report and the notify JSON. No tickets, no git, no config/data touched.

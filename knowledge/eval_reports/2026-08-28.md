# Weekly eval — 2026-08-28 — ABORTED (stale inputs, 58 days; 6th consecutive abort on the same snapshot)

Freshness gate failed. No evaluation performed, no tickets written.

## Input state

| Item | Value |
|---|---|
| Newest snapshot | `eval-inbox/tp_snap_2026-07-01.db` |
| Snapshot age | **58 days** (limit: 8) |
| `hermes-issues.json` | dated 2026-07-01, md5 `355455911bad72cf01de206d2dd9ca7c` — unchanged from prior weeks |
| Other snapshots anywhere in repo | none (`find . -name "tp_snap_*.db"` → 1 result) |
| Tickets written | 0 |
| `tickets/.ticket-counter` | `1009` (untouched) |
| `tickets/ready/` | empty |

## The notification channel is still dead — now 2 weeks confirmed

`eval-inbox/eval-notify.json` written by the **2026-08-21** run is still sitting there,
unconsumed — mtime `2026-08-21 19:08`, seven days old at the time of this run.

`scripts/eval/send-eval-notification.ps1:25` deletes that file after a successful send
(`Remove-Item $File -Force  # fire once`); the only path that leaves it in place is the `catch`
block. The file surviving another full week means the poller is either not firing or failing
every time.

**David has now almost certainly missed both the 08-14 and 08-21 pushes.** That is why the
operator actions recommended in those reports were not carried out — the reports were written,
the alarm never rang. This is the single highest-priority failure in the loop: an eval nobody
reads is an eval that does not exist.

This run overwrites the same file. If the poller is still dead, this push will not be delivered
either, and this report file is the primary artifact.

Confirmed again independently: this sandbox cannot send directly. Probing `scripts/eval/notify.py`
returned `ntfy: Tunnel connection failed: 403 Forbidden` and `email: Temporary failure in name
resolution` — exactly as `weekly-eval.md` documents. **Nothing was sent from here** (both channels
failed on the probe, so no spurious notification escaped either).

## Failure inventory — three jobs down, unchanged from last week

| Job | Where | Last known good | Status |
|---|---|---|---|
| `pull-eval-inputs.ps1` (Fri 21:00) | Task Scheduler | 2026-07-01 | **Broken 8 weeks** |
| `trade-pilot-weekly-eval` (Fri 22:00) | Cowork scheduled task | 2026-08-28 (this run) | **Running** — fired 08-14, 08-21, 08-28; gaps at 07-31, 08-07 |
| `send-eval-notification.ps1` (polling) | Task Scheduler | unknown, ≥ 2026-08-14 | **Broken ≥ 2 weeks** (two stale `eval-notify.json` generations) |

Aborted evals on the identical July 1 snapshot: 07-10, 07-17, 07-24, 08-14, 08-21, **08-28**.

The only component reliably working is the Cowork eval task itself — which is the one component
that cannot do anything useful without the other two.

## Recommended operator actions (in order — unchanged from 08-21, still not done)

1. **Fix the notification first.** Run `scripts\eval\send-eval-notification.cmd` by hand and read
   the output. If it throws, `notify.py` is the fault (check the ntfy topic and SMTP credentials
   in `.env`); if it exits silently with `eval-notify.json` still present, the Task Scheduler entry
   is not firing. Until this works, every future eval is invisible.
2. Run `scripts\eval\pull-eval-inputs.ps1` by hand and capture the error. It stopped cleanly on
   July 1 — most likely the Render/production DB path or credential, an expired GitHub token for
   the Hermes pull, or a moved destination path.
3. Check Task Scheduler history for the pull job and the notify poller.
4. Once fresh inputs land, re-run this eval **manually** — do not wait for Friday. Eight weeks of
   decisions are unexamined.

## Health / risk note

No claim is made here about the bot's funnel, fills, cost, token truncation, or circuit-breaker
state. The only data available is 58 days old; evaluating it would produce conclusions about
early July dressed up as conclusions about late August. Per the methodology, stale data is not
evaluated.

Stated plainly: **any regression since 2026-07-01 is currently undetected** — a change in fill
behaviour, a cost blowout, a circuit-breaker trip, or a silent scheduler crash would all look
identical from here, which is to say invisible. The bot should be treated as unmonitored for
eight weeks. Nothing in this report argues it is healthy; it argues we still cannot tell.

## Red-scope / do-not-auto-implement items

None newly triaged. Hermes issues were not re-evaluated — the input file is byte-identical to
content already reviewed in prior weeks. The July 1 macro-date issue carrying
`do-not-auto-implement` (FOMC/CPI schedule extrapolation, unverified) remains report-only and
unresolved, awaiting David's decision.

## Carried forward (unchanged, still open)

- `tickets/attended/1007-investigate-vz-position-no-trade-record.md`
- `tickets/attended/1008-decide-monthly-eval-retirement.md`

Both are human-gated and were not touched by this run.

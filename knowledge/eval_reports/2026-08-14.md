# Weekly eval — 2026-08-14 — ABORTED (stale inputs, 4th aborted run on the same snapshot)

Freshness gate failed. No evaluation performed, no tickets written.

## Input state

| Item | Value |
|---|---|
| Newest snapshot | `eval-inbox/tp_snap_2026-07-01.db` |
| Snapshot age | **44 days** (limit: 8) |
| `hermes-issues.json` | dated 2026-07-01, unchanged |
| `eval-inbox/` contents | `.gitkeep`, `hermes-issues.json`, `tp_snap_2026-07-01.db` — nothing else |
| Tickets written | 0 |

## Why this is worse than last week

Two separate jobs are now failing, not one.

1. **The pull job has not produced input since July 1.** `scripts/eval/pull-eval-inputs.ps1`
   (Friday 21:00, Task Scheduler) last dropped a snapshot on 2026-07-01. That is now four
   aborted evals on the identical file: 2026-07-10, 2026-07-17, 2026-07-24, 2026-08-14.

2. **The Cowork eval task itself missed two weeks.** `knowledge/eval_reports/` has no
   `2026-07-31.md` and no `2026-08-07.md`. Previous aborted weeks each left a report, so the
   absence of those two files means the scheduled Cowork run did not fire (or failed before
   writing) on those dates. This was not visible in prior reports and is a new finding.

The self-improving loop has produced no signal for six weeks. Nothing has been evaluated,
so nothing has been ticketed, so the nightly `/run-tickets` runner has had an empty queue.
Silence from this loop is currently indistinguishable from "no improvements needed" —
that is the thing to fix first.

## Recommended operator actions (in order)

1. Open Task Scheduler → history for the `pull-eval-inputs` job. Confirm whether it is firing
   at all, and capture the exit code / error.
2. Run `scripts/eval/pull-eval-inputs.ps1` by hand and read the failure. Most likely candidates,
   given it worked through July 1 and stopped cleanly: the Render/production DB path or
   credentials rotated, the GitHub token for the Hermes issue pull expired, or the destination
   path moved.
3. Check the Cowork scheduled task (`trade-pilot-weekly-eval`) for the 07-31 and 08-07 gaps —
   machine asleep, task disabled, or session failure.
4. Once fresh inputs land, re-run this eval manually rather than waiting for next Friday;
   six weeks of decisions are unexamined.

## Health / risk note

The bot itself is not implicated by this report — no claim is made here about whether it is
trading, skipping, or burning tokens, because the only data available is 44 days old and
evaluating it would produce conclusions about July presented as conclusions about August.
Per the methodology, stale data is not evaluated. That also means **any regression since
July 1 is currently undetected**, including a change in fill behaviour or cost. Treat the
bot as unmonitored until the pull job is restored.

## Carried forward (unchanged, still open)

- `tickets/attended/1007-investigate-vz-position-no-trade-record.md`
- `tickets/attended/1008-decide-monthly-eval-retirement.md`

Both are human-gated and were not touched by this run.

# Weekly eval — 2026-08-21 — ABORTED (stale inputs, 51 days; 5th abort on the same snapshot)

Freshness gate failed. No evaluation performed, no tickets written.

## Input state

| Item | Value |
|---|---|
| Newest snapshot | `eval-inbox/tp_snap_2026-07-01.db` |
| Snapshot age | **51 days** (limit: 8) |
| `hermes-issues.json` | dated 2026-07-01, byte-identical to prior weeks |
| `eval-inbox/` contents | `.gitkeep`, `eval-notify.json` (stale, see below), `hermes-issues.json`, `tp_snap_2026-07-01.db` |
| Other snapshots found anywhere in repo | none (`find . -name "tp_snap_*.db"` → 1 result) |
| Tickets written | 0 |

## NEW this week: the notification channel is also dead

`eval-inbox/eval-notify.json` from **last week's run is still sitting there, unconsumed** —
mtime `2026-08-14 19:07`, seven days old.

Per `scripts/eval/send-eval-notification.ps1:25`, the local poller deletes that file after a
successful send (`Remove-Item $File -Force  # fire once`). Its only other exit path is the
`catch` block, which deliberately leaves the file for retry. So the file surviving a full week
means one of exactly two things:

1. The local poller task is not running at all, or
2. It is running and `notify.py` is failing every time (ntfy + SMTP both down / credentials
   rotated), silently leaving the file behind.

Either way: **David almost certainly never received the 2026-08-14 push.** That is the
explanation for why last week's recommended actions were not carried out — the report was
written, but the alarm never rang.

This run's notification is written to the same file, so if the poller is dead it will not be
delivered either. Treat this report file as the primary artifact.

Confirmed independently: this sandbox cannot send directly. Probing `scripts/eval/notify.py`
returned `ntfy: Tunnel connection failed: 403 Forbidden` and `email: Temporary failure in name
resolution` — exactly as `weekly-eval.md` documents. No notification was sent from here.

## Failure inventory — three jobs, not one

| Job | Where | Last known good | Status |
|---|---|---|---|
| `pull-eval-inputs.ps1` (Fri 21:00) | Task Scheduler | 2026-07-01 | **Broken 7 weeks** |
| `trade-pilot-weekly-eval` (Fri 22:00) | Cowork scheduled task | 2026-08-21 (this run) | **Intermittent** — no reports for 07-31, 08-07 |
| `send-eval-notification.ps1` (polling) | Task Scheduler | unknown | **Broken ≥ 1 week** (stale `eval-notify.json`) |

Aborted evals on the identical July 1 snapshot: 07-10, 07-17, 07-24, 08-14, 08-21.

## Recommended operator actions (in order)

1. **Fix the notification first.** Run `scripts\eval\send-eval-notification.cmd` by hand and read
   the output. If it throws, `notify.py` is the fault (check ntfy topic + SMTP creds in `.env`);
   if it exits silently with the file still present, the Task Scheduler entry is not firing.
   Until this works, every future eval is invisible to you.
2. Run `scripts\eval\pull-eval-inputs.ps1` by hand and capture the error. It stopped cleanly on
   July 1 — most likely the Render/production DB path or credential, an expired GitHub token for
   the Hermes pull, or a moved destination path.
3. Check Task Scheduler history for both the pull job and the Cowork weekly task (the 07-31 /
   08-07 gaps suggest the machine was asleep or the task was disabled).
4. Once fresh inputs land, re-run this eval **manually** rather than waiting for Friday. Seven
   weeks of decisions are unexamined.

## Health / risk note

No claim is made here about the bot's funnel, fills, cost, or circuit-breaker state. The only
data available is 51 days old; evaluating it would produce conclusions about early July dressed
up as conclusions about late August. Per the methodology, stale data is not evaluated.

The consequence stands and is worth stating plainly: **any regression since 2026-07-01 is
currently undetected** — including a change in fill behaviour, a cost blowout, or a circuit
breaker trip. The bot should be treated as unmonitored for seven weeks. Nothing in this report
argues it is healthy; it argues we cannot tell.

## Red-scope / do-not-auto-implement items

None triaged this run — Hermes issues were not evaluated because the input file is stale and
identical to previously-reviewed content. The July 1 macro-date issue carrying
`do-not-auto-implement` (FOMC/CPI schedule extrapolation, unverified web lookup) remains
report-only and unresolved.

## Carried forward (unchanged, still open)

- `tickets/attended/1007-investigate-vz-position-no-trade-record.md`
- `tickets/attended/1008-decide-monthly-eval-retirement.md`

Both are human-gated and were not touched by this run. `tickets/.ticket-counter` remains at
`1009`; `tickets/ready/` remains empty.

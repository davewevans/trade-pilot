# Weekly evaluation — 2026-07-01

**Snapshot:** `eval-inbox/tp_snap_2026-07-01.db` (captured 2026-07-01, fresh — freshness gate PASS)
**Hermes input:** `eval-inbox/hermes-issues.json` (2 issues, both `do-not-auto-implement`)
**Prior report:** none (this is the first eval report — no rolling baseline yet)

## TL;DR

**Tickets written this week: 0.** No green-scope, code-grounded, not-already-fixed defect surfaced. This is a legitimate "nothing to ticket" week.

**Two operator actions flagged (not code tickets — nothing to change in the repo):**
1. **The advisor is force-SKIPping 100% of `claude-sonnet-5` calls due to output truncation.** All 4 sonnet-5 calls this week hit `stop_reason: max_tokens` at exactly 2048 output tokens → forced SKIP. The code default `ADVISOR_MAX_TOKENS` is **already 4096** (config.py:325), so the running bot is being pinned to 2048 by a stale Render env var. **Raise/remove `ADVISOR_MAX_TOKENS` in the Render environment** (it's `os.getenv("ADVISOR_MAX_TOKENS", "4096")` — an env value of `2048` overrides the correct default).
2. **`claude-sonnet-5` pricing entry is missing from the repo's `CLAUDE_PRICING`** (config.py:979-988, still a commented `TODO(david)`). The *deployed* bot already computes sonnet-5 cost correctly (snapshot has non-null costs), so prod isn't broken — but the repo is out of sync. Paste your verified per-MTok rates into `CLAUDE_PRICING` and commit. Left report-only because it needs verified official rates you own, not a value the runner should guess.

**Report-only (do-not-auto-implement):** Hermes macro-date issues #63/#67 (near-duplicates → treated as one). Verified dates below.

---

## Step 1 — DB analysis (the week's truth)

### Funnel (decisions table)

| Window | Total | Actions |
|---|---|---|
| All-time (since 2026-04-13) | 864 | SKIP 857, SELL_PUT 7 |
| Trailing 7d | 146 | SKIP 146 (0 trade actions) |

**Skip reasons, trailing 7d:**

| skip_reason_code | count | gate |
|---|---|---|
| structurally_untradeable | 102 (70%) | pre_check |
| claude_skip | 39 (27%) | claude_skip |
| no_candidates_found | 5 (3%) | no_candidate |

By strategy (7d): wheel 71, turnover_wheel 70, iron_condor 3, bull_put_spread 2 — all SKIP. `structurally_untradeable` (pre-check gate rejecting symbols before Claude even sees them) remains the dominant funnel loss, consistent with the known watchlist/structural baseline. Not a defect — reported as context.

### Fills

**Zero fills, still.** `trades` has 5 rows total (4 canceled, 1 pending), all submitted 2026-05-19 → 2026-06-12; none filled. Zero fills in the trailing 7 days and zero across the bot's entire life. Baseline **unchanged** — flag only if this ever moves.

### Cost (token_usage, trailing 7d)

- Total: **$1.28** (all-time $13.47)
- By model: `claude-sonnet-4-6` $1.04 (35 calls), `claude-sonnet-5` $0.24 (4 calls)
- Note: the switch to sonnet-5 as default `ADVISOR_MODEL` is visible — only 4 sonnet-5 calls so far, all today (2026-07-01).

### Health flags

- **`max_tokens` truncation: 4 decisions, all trailing 7d, all sonnet-5.** IDs 853/854/863/864 (wheel + turnover_wheel on F, T). Each: `output_tokens=2048`, `reasoning` = `"Claude stop_reason: max_tokens"`, `action=SKIP`. **100% of sonnet-5 calls truncated.** See operator action #1 — the fix (default 4096) is already in code; the live bot is pinned to 2048 by env. `claude-sonnet-4-6` calls (which peak ~1790 output tokens) are unaffected.
- **Orphan trade-action decisions: 2** (IDs 183 F, 209 VZ — both SELL_PUT, 2026-05-13/14, no `trades` row, no `alpaca_order_id`). Both predate the 2026-05-19 context-builder account-isolation fix and are in the known recording-gap class. Only 2 samples, stale, and this is execution/order-recording territory (red scope). Report-only.
- **decisions.estimated_cost_usd: NULL for all 864 rows** (also model_version/tokens NULL on that table). Already handled — `CLAUDE_COSTS_SOURCE` defaults to `token_usage` (config.py:329), which is populated. No action.
- **Circuit breaker:** no anomalies observed in the snapshot; no new-entry blocks attributable to CB this week (skips are pre_check/claude, not CB).

---

## Step 2 — Hermes issues

Two issues, **#63** (2026-06-30) and **#67** (2026-07-01), both titled "[hermes] macro: propose missing FOMC/CPI dates", both carrying **`do-not-auto-implement`**. They are near-duplicates (same coverage gap, slightly different extrapolated dates) → **deduped to one candidate → REPORT ONLY.** Never auto-ticketed.

Hermes itself flagged that its web lookup failed and its proposed dates are **extrapolated/unverified**. I verified against the official sources:

### FOMC — VERIFIED (Fed press release 2025-09-05, tentative 2027 schedule)

Decision-day dates (second day of each meeting; macro_events.json uses 14:00 ET on the decision day):

- 2027-01-27, 2027-03-17, 2027-04-28, 2027-06-09, 2027-07-28, 2027-09-15, 2027-10-27, 2027-12-08
- 2028-01-26 (first 2028 meeting; **the only 2028 FOMC date officially published so far**)

⚠️ Hermes's file reportedly already has FOMC coverage through Dec 2027, so the real gap is 2028. **Only 2028-01-26 is official.** Hermes's other proposed 2028 dates (Mar 15, Apr 26, Jun 14, …) are extrapolations — the Fed has **not** published the rest of the 2028 schedule. Do not apply the unverified 2028 dates.

### CPI — NOT YET PUBLISHED for 2027

⚠️ The BLS CPI release schedule (bls.gov/schedule/news_release/cpi.htm) currently runs only through **November 2026** (released 2026-12-10). BLS has **not** published any 2027 CPI dates yet. **Every 2027 CPI date in Hermes #63/#67 is an unverifiable extrapolation.** Recommend not adding 2027 CPI dates until BLS publishes the official 2027 schedule (typically released in the second half of the prior year — watch for it late 2026).

**Net recommendation for the operator:** hold both Hermes issues. The near-term coverage (through Dec 2026 CPI / Dec 2027 FOMC) is intact; the proposed forward dates are either unverifiable now (2027 CPI, 2028 FOMC beyond January) or already covered. Re-evaluate once BLS/Fed publish official schedules.

---

## Step 3–4 — Triage outcome

| Candidate | Scope | Grounded in code? | Outcome |
|---|---|---|---|
| sonnet-5 max_tokens truncation | config / model | Fix already in code (default 4096) | **Drop as ticket** — operator env action (Render `ADVISOR_MAX_TOKENS`) |
| sonnet-5 pricing missing in repo | cost/observability (green-ish) | Real repo gap; deployed already correct; needs verified rates (`TODO(david)`) | **Report-only** — needs operator-owned verified rates |
| decisions cost columns NULL | observability | Already handled (`CLAUDE_COSTS_SOURCE=token_usage`) | Drop |
| Hermes macro dates #63/#67 | data (macro_events.json) | `do-not-auto-implement` | **Report-only** — verified dates above |
| Orphan SELL_PUT decisions (2) | execution/recording | Stale (pre-2026-05-19 fix), red scope, n=2 | Report-only |
| structurally_untradeable dominance | funnel baseline | Known structural constraint, not a defect | Reported as context |

**Tickets written: 0.** `.ticket-counter` unchanged (still 1002).

---

## Items awaiting David's decision (red / report-only)

1. **[Operator/env, high impact]** Raise `ADVISOR_MAX_TOKENS` in the Render environment to ≥ 4096 (matching the code default). Until then the advisor force-SKIPs every sonnet-5 decision on `max_tokens` truncation.
2. **[Repo hygiene]** Add the verified `claude-sonnet-5` entry to `CLAUDE_PRICING` (config.py) and commit — the deployed bot already prices it, but the repo `TODO(david)` is still open. If you provide the rates, this is a clean green ticket next week.
3. **[Report-only]** Hermes #63/#67 — hold; forward FOMC-2028/CPI-2027 dates unverifiable until officially published (details above).

## Sources
- Fed FOMC 2027 tentative schedule: https://www.federalreserve.gov/newsevents/pressreleases/monetary20250905a.htm
- BLS CPI release schedule: https://www.bls.gov/schedule/news_release/cpi.htm
